问题1
02 发送邮件时
文件后缀不可被发送的：
目前已知的是 .tmp .mat .mprx .py 这几种
.zip .rar 就不尝试了，直接跳过
上述几种可以修改的格式，在后面直接加 .txt，并记录在log中 (wrapped_extensions.txt)

这个log要随着最后一封，邮件一并发送



03 脚本恢复目录结构时

问题2
会出现路径过长问题 （根目录 / package_日期时间 / 相对路径）总体太长
导致文件在package中以四位数字形式存在，但无法恢复成完整文件名

可能的方案：

遇到此报错时：
如果是文件名太长，将文件名本身精简（只保留前4个字符+序号），并在输出的log中记录原文件名，一一对照
如果文件夹太长，就需要一层一层向前迭代，每一次都将最后一层精简到4个字符+序号，也输出在log中
为了测试，可以先内置一个长度阈值比如20，更容易发现问题

当前的办法：将目标文件名精简后，人工恢复 


问题3
读取到 wrapped_extensions.txt 时，将其中的文件名后缀恢复
wrapped_extensions.txt 也保存一份

问题4
output_log txt 目前是将 03 脚本在控制台的输出直接手动复制出来的，需要能够自动保存